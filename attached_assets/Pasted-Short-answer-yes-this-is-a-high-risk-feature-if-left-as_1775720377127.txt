Short answer: **yes — this is a high-risk feature if left as-is**, but it’s **not inherently non-compliant**. It just needs the right controls.

Let’s break it down properly.

---

# 🔴 WHAT YOU BUILT

**Internal transfer (email-based, instant, zero fee, off-chain)**

* BTC is **not actually moving on blockchain**
* It’s **ledger-to-ledger inside AMAX**
* Recipient identified by **email only**

👉 This is effectively:

> **a closed-loop value transfer system (like PayID / Revolut / Wise internal transfer)**

---

# ⚠️ WHY THIS IS PROBLEMATIC (CURRENT STATE)

## 1. ❌ Email ≠ verified beneficiary identity

Right now:

```text
Send BTC → email address
```

Problem:

* Email alone is **not a verified financial identifier**
* Could be:

  * Wrong recipient
  * Compromised account
  * Mule account

👉 AUSTRAC expectation:

> You must **identify and verify the beneficiary**, not just route by string input

---

## 2. ❌ Bypasses Travel Rule (if crypto-labelled)

Even though it’s “internal”:

* User thinks: “I sent BTC”
* Regulator sees: “value transfer between customers”

If you later:

* allow withdrawal to external wallet
  👉 this becomes a **layering risk**

---

## 3. ❌ High ML typology risk (VERY IMPORTANT)

This exact feature is commonly used for:

* Rapid layering
* Mule account distribution
* Smurfing across accounts

Example:

```text
Account A → split → 10 emails → withdraw
```

👉 Your current system:

* No friction
* No beneficiary validation
* No velocity controls (based on what you've shown)

---

## 4. ❌ No transaction purpose / context

You earlier asked about removing “purpose of transfer”

👉 For **internal transfers**, that becomes riskier

Because:

> There is **no external banking narrative** to rely on

---

## 5. ❌ Not classified in your system

Your system currently does NOT tag:

```text
internal_transfer vs external_transfer vs crypto_transfer
```

👉 That breaks:

* Monitoring
* Reporting logic
* Risk scoring

---

# ✅ WHEN THIS IS OK (COMPLIANT VERSION)

This feature is **allowed** IF you treat it as:

> **Domestic value transfer within a closed-loop system**

Same as:

* Wise internal transfers
* Revolut user-to-user

---

# 🛠 REQUIRED FIXES (NON-NEGOTIABLE)

## ✅ 1. Replace “email only” with VERIFIED RECIPIENT

Instead of:

```text
Enter email
```

You must:

### Option A (best)

* Select from:

  * Saved recipients
  * Verified AMAX users

### Option B

* Input email → system resolves:

```ts
const recipient = await findUserByEmail(email);

if (!recipient || recipient.kycStatus !== "verified") {
  throw new Error("Recipient must be a verified AMAX user");
}
```

---

## ✅ 2. Enforce BOTH SIDES KYC

```ts
if (sender.kycStatus !== "verified") block;
if (recipient.kycStatus !== "verified") block;
```

👉 No exceptions

---

## ✅ 3. Add INTERNAL TRANSFER CLASSIFICATION

In transaction model:

```ts
type: "internal_transfer"
subType: "crypto" | "fiat"
channel: "closed_loop"
```

---

## ✅ 4. Apply AML RULES SPECIFICALLY

Add rules like:

```ts
Rule: rapid internal transfers between multiple accounts
Rule: fan-out pattern (1 → many)
Rule: circular transfers (A → B → A)
Rule: dormant account receiving funds
```

---

## ✅ 5. Velocity + Limits (VERY IMPORTANT)

```ts
dailyInternalTransferLimit = based on riskScore
```

Example:

| Risk   | Limit    |
| ------ | -------- |
| Low    | $50k/day |
| Medium | $20k/day |
| High   | $5k/day  |

---

## ✅ 6. Add LIGHT PURPOSE / REASON TAG (DON’T REMOVE COMPLETELY)

Instead of free text:

```tsx
<Select>
  <Option>Personal transfer</Option>
  <Option>Business payment</Option>
  <Option>Family support</Option>
</Select>
```

👉 This helps:

* Monitoring
* SMR context

---

## ✅ 7. Pre-Transfer Warning (IMPORTANT UX + LEGAL)

```text
Transfers are irreversible.
Only send to trusted, verified AMAX users.
```

---

## ✅ 8. LINK TO WITHDRAWAL MONITORING

Critical:

If flow is:

```text
Internal transfer → immediate withdrawal
```

👉 Auto-flag:

```ts
if (internalTransfer && withdrawalWithin < 1 hour) {
  flag = "rapid_outflow";
}
```

---

# ⚖️ ANSWER TO YOUR ORIGINAL INTENT

> “Zero fee internal BTC transfer — is it okay?”

### ✅ YES — IF:

* Both users are fully KYC’d
* It’s treated as **internal ledger movement**
* It is **monitored like remittance behaviour**

### ❌ NO — IF:

* Email-only routing
* No beneficiary validation
* No AML controls
* Used as crypto abstraction layer

---

# 🔥 FINAL VERDICT

| Area                   | Status           |
| ---------------------- | ---------------- |
| Product idea           | ✅ Strong         |
| Current implementation | 🔴 Risky         |
| Fix complexity         | 🟡 Medium        |
| Regulatory viability   | ✅ Yes (if fixed) |

---

# 👍 If you want next step

I can:

### 🔧 Write exact patch for:

* email → verified user resolution
* internal transfer AML rules
* velocity limits

OR

### 🧠 Design full **internal transfer compliance engine**

Just say:
👉 “patch internal transfer” or “design engine”
